# ============================================================
# GreenOps — Environment Variables
# ============================================================
# Copy to .env and fill in values.
# NEVER commit .env to version control.
#
# Only infrastructure secrets belong here.
# ALL runtime configuration (idle_threshold, electricity_cost, etc.)
# is now stored in the database (app_settings table).
# Use the Settings UI or PUT /api/settings to change runtime config.
# ============================================================

# ── Database ────────────────────────────────────────────────
# PostgreSQL connection URL.
# Format: postgresql://USER:PASSWORD@HOST:PORT/DBNAME
DATABASE_URL=postgresql://greenops:changeme@localhost:5432/greenops

# Used by docker-compose to set DB password
POSTGRES_PASSWORD=changeme

# ── Security (required) ─────────────────────────────────────
# Long random string for JWT signing. Generate with:
#   python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET_KEY=change-this-to-a-long-random-secret

# Flask session secret (can be same as JWT or different)
FLASK_SECRET_KEY=change-this-too

# ── Flask ────────────────────────────────────────────────────
FLASK_ENV=production
PORT=5000

# ── REMOVED — now in database app_settings table ─────────────
# IDLE_THRESHOLD_SECONDS     → use Settings UI
# HEARTBEAT_TIMEOUT_SECONDS  → use Settings UI
# IDLE_POWER_WATTS           → use Settings UI
# ELECTRICITY_COST_PER_KWH   → use Settings UI
# ORGANIZATION_NAME          → use Settings UI
# LOG_LEVEL                  → use Settings UI

